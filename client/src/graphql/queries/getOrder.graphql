query GetOrder($orderId: String!) {
  getOrder(orderId: $orderId) {
    _id
    total
    tracking
    createdAt
    timeline
    status
    adress {
      name
      line1
      line2
      phone
      email
      city
      postal_code
      country
    }
    user {
      email
    }
    products {
      quantity
      variant {
        title
        shoes {
          _id
          vendor
          title
          images {
            src
          }
        }
      }
    }
  }
}
