query GetShoes($limit: Float!, $page: Float!) {
  getFilterShoes(limit: $limit, page: $page) {
    pageInfo {
      total
      current
    }
    edges {
      ...ShoesBrowseFragment
      images {
        ...ImageFragment
      }
    }
  }
}
